pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'start /b mvn clean install'
                sh 'start /b mvn clean package'
            }
        }
    }
}

