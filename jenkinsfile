pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                checkout([$class: 'GitSCM', branches: [[name: '*/main']], extensions: [], userRemoteConfigs: [[credentialsId: 'token', url: 'https://github.com/vinuthapra/spring-boot-maven-demo.git']]])
            }
        }
        stage('Build') {
            steps {
                bat 'start /b mvn clean package -DskipTests'
            }
        }
        stage('Copy Jar to Build Directory') {
    steps {
        dir('target') {
            bat "dir target"
            bat "mkdir Build_${BUILD_ID}"
            bat "copy demo-0.0.1-SNAPSHOT.jar Build_${BUILD_ID}/"
        }
    }
}

    }
}


